<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java.circle.dao.CircleMapper">

	<insert id="addCircle" parameterType="HashMap" useGeneratedKeys="true" keyProperty="circle_id">
		insert into cCircle (name, univ_id, circle_category_id, intro, image) 
		values (#{name},#{univ_id},#{circle_category_id},#{intro},#{image})
	</insert>

	<select id="checkAddcircle"  parameterType="String" resultType="int">
		select count(*) as rowcount from cCircle where name=#{name}
	</select>
	
	<select id="showUnivInfo" parameterType="String" resultType="com.java.circle.dto.CDtoUniv">
		select * from cUniv where univ_id=(select univ_id from cUser where account=#{account})
	</select>
	
</mapper>